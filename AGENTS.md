# Agent context (ScaleBuds site)

Cursor rules live in **`.cursor/rules/*.mdc`** (always-on). Read those first for stack, GHL env, and paths.

- **Env template:** `.env.example`
- **Lead API:** `lib/leadApi.ts`, `api/submit-lead.ts` (keep in sync)
- **Chat widget:** `client/index.html`

If your workspace root is the parent `HVAC_Marketing_Website/` folder, additional rules may exist at `../.cursor/rules/`.
