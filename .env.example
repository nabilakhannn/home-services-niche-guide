# Go High Level — Automation → Workflow → Webhook (Inbound Webhook) trigger URL
# Paste the full https://… URL from GHL. Restart dev server after changing.
GHL_INBOUND_WEBHOOK_URL=

# Optional: pipeline id from GHL (Settings → Pipelines; id often looks like pit-…).
# Sent on each lead as JSON field ghlPipelineId for workflow mapping.
GHL_PIPELINE_ID=

# Public site URL (optional, used in articles/schema)
# VITE_PUBLIC_SITE_URL=https://scalebuds.com
